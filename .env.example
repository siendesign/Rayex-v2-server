# Server Configuration
PORT=3003
NODE_ENV=development

# Security
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# CORS Configuration
# For multiple origins, separate with comma: https://app1.com,https://app2.com
CORS_ORIGIN=http://localhost:3000

# Clerk Authentication
# Development: sk_test_...
# Production: sk_live_...
CLERK_SECRET_KEY=sk_test_your_clerk_secret_key

# Database Configuration (PostgreSQL)
# Railway will auto-populate this when you add PostgreSQL
DATABASE_URL=postgresql://postgres:password@localhost:5433/rayex?schema=public

# ============================================
# RAILWAY DEPLOYMENT NOTES
# ============================================
# 1. PORT - Railway provides this automatically (usually dynamic)
# 2. NODE_ENV - Set to "production" in Railway
# 3. JWT_SECRET - Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# 4. CORS_ORIGIN - Set to your frontend URL (e.g., https://rayex.vercel.app)
# 5. CLERK_SECRET_KEY - Use your production key from Clerk Dashboard (sk_live_...)
# 6. DATABASE_URL - Auto-populated by Railway PostgreSQL addon (don't override)

# Optional: External API Keys (for future features)
# EXCHANGE_RATE_API_KEY=your-exchange-rate-api-key
